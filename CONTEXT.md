# 领域术语表

> 本文档仅收录领域术语的语义定义，不含实现细节。

## 核心概念

### 医院（Hospital / Center）

提供临床数据的医疗机构。在本系统中，**医院与租户（Tenant）是同一概念**——每注册一家医院即创建一个对应租户，通过 `tenant_id` 实现行级数据隔离。

医院在历史数据中以 `source_center` 自由文本字段标识（如 `"省医"`、`"珠江-新桥"`），注册后与 `sys_tenant.code` 建立持久关联。

### 租户（Tenant）

平台的多租户隔离单元。系统租户（id=1）为平台管理，普通租户（id>1）对应各医院，独立管理用户、角色、权限、配额。

### 多中心数据（Multi-Center Data）

来自多家医院的临床数据，按统一表结构汇聚。分为两类：
- **统一表**（patient、pathology_specimen、surgery_record、genetic_test）：核心字段取并集，可直接跨院 UNION 查询
- **医院独有表**（如省医的 visit_record、珠江-新桥的 nodule_imaging）：仅单院存在，不参与跨院 UNION

### 来源中心（source_center）

患者记录中的自由文本字段，标识数据来自哪家医院。与 `sys_tenant.code` 冗余存在——前者为历史数据兼容，后者为数据来源标记。

> **注意**：`tenant_id` 在医疗表中仅作为数据来源标记，**不作为访问控制边界**。所有登录用户可查询全部医院的数据（当前阶段不脱敏），可按医院做可选筛选。

### 模态（Modality）

患者数据按医学维度分为四类：临床（clinical）、基因（genetic）、病理（pathology）、影像（imaging）。用于前端分组展示和数据筛选。

### 统一表 Schema

多中心数据的标准化字段定义。核心字段取并集（缺的一方填 null），JSON 扩展列各自独立（不强制统一）。

### 数据层（Data Layer）

医疗业务数据的存储与访问层。当前处于从 DuckDB 直读 parquet 向 PostgreSQL 全量入库的迁移过程中。

### 医院筛选维度（Hospital Filter）

前端展示数据时，用户可**选择是否按医院筛选**。默认可见全部医院数据，也可缩小到单家或多家属性和。
这仅是查询维度的切换，**不影响数据权限**——所有登录用户的可见数据范围相同。

### 平台超级管理员

`is_superuser=1` 且 `tenant_id=1` 的用户。可执行平台级操作：注册医院、配置映射、触发导入等。
超级管理员在医疗数据查询层面与其他用户一致——全部可见。

### 插件（Plugin）

平台的功能模块单元，由 `sys_plugin` 注册表和 `sys_tenant_plugin` 租户安装关联表管理。医院（租户）可独立安装/启用插件。

### ETL 导入

将医院原始数据（parquet 文件）转换为统一表结构并写入 PostgreSQL 的过程。与医院注册分离，作为独立后台任务执行。

### Schema 映射（Mapping）

定义医院原始数据字段到统一表字段的对应关系和转换规则。粒度为 **(源表, 源字段) → (目标表, 目标字段, 转换规则)**。

### 映射模板

预定义的 Schema 映射集合。系统携带已知医院的完整模板（省医、珠江-新桥），注册新医院时可基于模板预填映射，管理员确认或微调。

### 医院就绪状态

医院元数据经历的生命周期阶段。顺序为：已注册 → 映射已配置 → 数据已导入 → 已上线。仅"已上线"状态医院的数据可供查询。

### 医院注册操作

仅平台超级管理员可执行的平台级操作。发生在新建独立模块（医院管理）中，注册流程内部调用租户服务创建对应租户。

### 医院管理模块

独立业务模块（`module_medical/hospital/`），包含医院注册表和映射配置表。与通用租户管理模块分离——租户是平台基础设施，医院是业务实体。

### PostgreSQL 共享表模式

所有医院（租户）的医疗数据存储在相同的表结构中，通过 `tenant_id` 列区分所属医院。统一表（patient、pathology_specimen 等）支持跨院 UNION 查询。

### 映射引擎（ETL 引擎）

通用数据转换框架：读取映射配置 → 执行转换 → 写入 PostgreSQL。能力上限为简单映射（重命名/常量/值变换）可配置化；结构转换（行展开/跨表关联）硬编码在固定 ETL 任务中。
